'use client'

import { Button, Input } from "@yamada-ui/react";


export default function TverURLForm() {
  return (
    <form>
      <Input placeholder="TVerのURLを入力" />
      <Button>Click me!</Button>
    </form>
    )
}